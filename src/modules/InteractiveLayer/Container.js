import React, { Component } from 'react'
import InteractiveLayer from './InteractiveLayer'
import { connect } from 'react-redux'

const mapStateToProps = (state) => {
  
}

export default connect(mapStateToProps)(InteractiveLayer)
